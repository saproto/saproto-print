module.exports = {

    // The port the HTTP server listens on.
    host: '130.89.190.22',
    port: 12345,

    // The IP addresses that are allowed to use this server.
    ips: [
        '37.97.129.76'
    ],

    // The secret that needs to be supplied in order to print.
    secret: 'ThisTokenIsNotSoSecretChangeIt',

    // The printers that are available for printing.
    printers: {
        document: 'Brother HL-2250DN',
        card: 'Zebra ZXP Series 1 USB Card Printer'
    },

    // The SumatraPDF executable file.
    sumatra: "C:\\Program Files (x86)\\SumatraPDF\\SumatraPDF.exe"

};